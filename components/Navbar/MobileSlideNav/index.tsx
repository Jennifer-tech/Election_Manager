import React from 'react'

type Props = {
  toggle: boolean;
  setToggle: (x: boolean) => void;
}
const MobileSlideNav = ({ toggle, setToggle}: Props) => {
  return (
    <div>MobileSlideNav</div>
  )
}

export default MobileSlideNav